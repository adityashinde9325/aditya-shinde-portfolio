
# Force HTTPS and non-www redirection
RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\. [NC]
RewriteRule ^(.*)$ https://adityashinde.tech/$1 [L,R=301]

# Redirect HTTP to HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://adityashinde.tech/$1 [L,R=301]
